// script.js

function navigateTo(game) {
    alert(`Você escolheu jogar: ${game}`);
    // Aqui, você pode adicionar a lógica de navegação real,
    // por exemplo, redirecionar para outra página ou abrir uma modal com o jogo.
    // Para este exemplo, vamos apenas simular com um alerta.

    // Para navegação para páginas de jogo (se houver páginas separadas):
    // window.location.href = `${game.toLowerCase()}.html`;
}
